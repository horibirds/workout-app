# 筋トレ計測アプリ 仕様書

## 1. プロジェクト概要

スマートフォン（iPhone）のIMUセンサーを使って筋トレの「質」を定量化するアプリ。
「30回を速くやる」より「10回を丁寧にやる」方が高スコアになる指標設計を目指す。

---

## 2. デバイス構成

### メインデバイス: iPhone
- **設置方法**: バーベルシャフト（またはスクワットバー）にマウント
- **使用センサー**:
  - **加速度計（Accelerometer）**: 加速度・移動量の計測
  - **ジャイロスコープ（Gyroscope）**: バーの傾き・回転検出
  - **カメラ**: トレーニング種目の自動認識（オプション）

### 将来拡張（Phase 2）
- **M5StickC**: iPhoneと並行してバーに設置し、より精度の高いIMUデータ取得
- **FSRセンサー**: グリップ力・荷重の直接計測（スマホだけで不十分な場合）

### iPhoneのみで対応できる理由
- iPhone 12以降のCore Motionは精度の高い6軸IMU（加速度+ジャイロ）を内蔵
- バーの上下運動（鉛直方向）の加速度積分で変位・速度推定が可能
- カメラ + Vision frameworkで姿勢・種目認識が可能

---

## 3. 仕事量の定義

### 3.1 物理的な問題点

通常の力学的仕事 `W = F・d` では、バーを上げて下げると往復でほぼゼロになる。
これでは「丁寧にゆっくり上げた」ことが評価されない。

### 3.2 採用する指標：「絶対仕事量（Absolute Work）」

```
W_abs = ∫ |F(t)| · |v(t)| dt  ≈  ∫ |a(t) + g| · |v(t)| dt · m_eff
```

- `a(t)`: 鉛直方向加速度（IMU計測）
- `v(t)`: 鉛直方向速度（加速度の積分）
- `g`: 重力加速度 9.81 m/s²
- `m_eff`: 有効質量（バー重量 + プレート重量、ユーザー入力）

**符号を取らずに絶対値で積算**するため、上げ・下げ両方が仕事量に加算される。

### 3.3 速度依存ペナルティ（質の評価）

速く動かすと筋への負荷が抜ける現象（ストレッチショートニングサイクルの過剰利用）を捉えるため、
**速度ペナルティ係数**を導入：

```
Q_score = W_abs · penalty(v_max)

penalty(v_max) = exp(-k · v_max)
  k ≈ 2.0（チューニングパラメータ）
  v_max: そのレップの最大速度 [m/s]
```

- ゆっくり（v_max < 0.3 m/s）: penalty ≈ 0.55〜1.0 → 高スコア
- 速い（v_max > 1.0 m/s）: penalty ≈ 0.14 → 低スコア

### 3.4 レップの計測フロー

```
1. 静止状態検出（開始位置）
2. 上昇フェーズ開始（加速度 > 閾値）
3. 頂点検出（速度≈0）
4. 下降フェーズ
5. 静止状態検出（終了） → 1レップ完了
```

各フェーズで `W_abs` を積算し、フェーズ別内訳も記録。

### 3.5 セッション総合スコア

```
Session_Score = Σ(Q_score_i)  for i in reps

Efficiency = Session_Score / (総時間[s])
```

---

## 4. トレーニング種目の自動認識

### 4.1 IMUベースの認識（優先）

| 種目 | バーの動き | IMU特徴 |
|------|------------|---------|
| ベンチプレス | 鉛直上下 | Y軸主成分、傾きほぼ水平 |
| スクワット | 鉛直上下 | Y軸主成分、バー水平保持 |
| デッドリフト | 鉛直上下 | 加速度パターンが異なる |
| ショルダープレス | 鉛直上下 | ベンチより振れが大きい |
| バーベルロウ | 水平+鉛直 | 複合軸運動 |

### 4.2 カメラベースの認識（オプション）

- Vision framework + Core ML による姿勢推定
- セットアップ時に1フレームでユーザーポーズを判定
- ベンチ（仰向け）/ スクワット（直立）/ デッドリフト（前傾） を分類

---

## 5. アプリ機能

### 5.1 セッション計測画面

- リアルタイム表示:
  - 現在の速度 [m/s]
  - 今レップの仕事量 [J]
  - レップカウンター
  - 速度バー（ゆっくり=緑、速い=赤）
- セット設定:
  - バー重量 + プレート重量入力
  - 目標レップ数

### 5.2 レポート画面

#### セット単位レポート
- レップごとの棒グラフ（Q_score）
- 速度vs仕事量の散布図
- 上昇/下降フェーズ別仕事量内訳

#### セッション単位レポート
- 総仕事量・効率スコア推移
- レップ速度の時系列グラフ（疲労検出）
- 種目別サマリー

#### 長期トレンド（履歴）
- 週次・月次の総仕事量推移
- 種目別パフォーマンス推移
- Personal Recordの管理

### 5.3 種目管理
- プリセット種目（ベンチプレス、スクワット、デッドリフト等）
- カスタム種目の追加

---

## 6. 技術スタック

### iOSアプリ
- **言語**: Swift
- **フレームワーク**:
  - `Core Motion`: IMUデータ取得（100Hz）
  - `Vision` / `Core ML`: 種目認識
  - `SwiftUI`: UI
  - `Charts`（Swift Charts）: グラフ表示
  - `SwiftData`（または Core Data）: ローカルデータ保存

### データフロー
```
IMU (100Hz) → バッファリング → フィルタリング（ローパス）
→ 速度積分（ドリフト補正付き）→ レップ検出 → 仕事量計算
→ UI更新（30Hz）/ DB保存
```

---

## 7. 技術的課題と対策

### 課題1: 加速度積分のドリフト
- **問題**: 加速度を積分すると誤差が累積して速度がずれる
- **対策**: 静止フェーズ（レップ間）でゼロ速度補正（ZUPTアルゴリズム）

### 課題2: 有効質量の不明
- **問題**: iPhoneだけでは荷重を直接計測できない
- **対策**: ユーザーが重量を手動入力（バー重量 + プレート重量）。スコアは相対比較に使用

### 課題3: バーへのiPhone固定
- **問題**: 安定したマウントが必要
- **対策**: バーベルクランプ型スマホホルダーを使用（市販品）

### 課題4: 速度推定精度
- **問題**: スマホIMUの精度限界
- **対策**: カルマンフィルタで加速度計とジャイロを融合。低速域（< 0.1 m/s）の閾値設定

---

## 8. 開発ステップ

### Phase 1: プロトタイプ（MVP）
- [ ] Core Motionで加速度・ジャイロデータ取得
- [ ] 速度・変位の積分計算（ZUPTドリフト補正）
- [ ] レップ自動検出
- [ ] 絶対仕事量（W_abs）の計算
- [ ] Q_score（速度ペナルティ付き）の計算
- [ ] 基本UI（リアルタイム速度・仕事量表示）

### Phase 2: 計測精度向上
- [ ] カルマンフィルタの実装
- [ ] ペナルティ係数kのキャリブレーション機能
- [ ] バー固定位置の補正（iPhoneがバーの端にある場合の回転補正）

### Phase 3: レポート機能
- [ ] セット・セッションレポート画面
- [ ] Swift Chartsによるグラフ実装
- [ ] SwiftDataによるデータ永続化
- [ ] 長期トレンド表示

### Phase 4: 種目認識
- [ ] IMUパターンによる種目自動認識
- [ ] カメラ + Vision frameworkによる姿勢認識（オプション）

### Phase 5: 拡張
- [ ] M5StickC連携（BLE）
- [ ] FSRセンサー連携（荷重直接計測）
- [ ] Apple Watch連携（心拍数との相関）

---

## 9. 仕事量計算の実装例（疑似コード）

```swift
// レップ中の仕事量積算
var W_abs: Double = 0.0
var velocity: Double = 0.0

func processIMUSample(acceleration: Double, dt: Double) {
    // 重力補正済み加速度
    let netAcceleration = acceleration // 静止時ゼロになるよう補正済み

    // 速度積分
    velocity += netAcceleration * dt

    // 絶対仕事量: |F| * |v| * dt = m * |a+g| * |v| * dt
    let force = mass * abs(netAcceleration + g)
    W_abs += force * abs(velocity) * dt
}

// Q_score計算
func calculateQScore(W_abs: Double, v_max: Double) -> Double {
    let k = 2.0
    let penalty = exp(-k * v_max)
    return W_abs * penalty
}
```

---

## 10. 成功指標

| 指標 | 目標 |
|------|------|
| レップ検出精度 | > 95% |
| 速度推定誤差 | < 0.05 m/s（対比較器） |
| アプリ遅延 | < 100ms（リアルタイム表示） |
| スコアの再現性 | 同条件で ±5% 以内 |
