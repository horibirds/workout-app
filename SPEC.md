# 筋トレ計測アプリ 仕様書

## 1. プロジェクト概要

### 解決したい問題

パーソナルトレーニングなどで「10回やりましょう」と言われたとき、
ゆっくり丁寧にやって8回で止まると「残念」な空気になる。
しかし実際は、8回ゆっくり丁寧にやった方が筋肉への総負荷は高い可能性がある。

**「回数」ではなく「筋肉にかかった総負荷の質」で評価することで、
丁寧なトレーニングが正しく報われるようにする。**

### アプリが目指すこと

- 速く10回やった人より、ゆっくり丁寧に8回やった人の方がスコアが高くなる
- 「8回しかできなかった」ではなく「高品質な8回だった」と表示する
- 回数目標に縛られず、自分のペースで丁寧にやり切ることを促進する

### デバイス

iPhoneをバーベルシャフトにマウントし、内蔵IMU（加速度計＋ジャイロ）で計測。
専用ハードウェア不要。

---

## 2. 「速く10回 vs ゆっくり8回」の物理的根拠

### なぜ速いと負荷が抜けるか

**① 力-速度関係（Hill方程式）**

筋肉は収縮速度が上がるほど発揮できる力が双曲線的に低下する。
つまり「速く動かす = 筋肉が出せる力が下がる = 実質的な負荷が下がる」。

**② デセラレーションフェーズの問題**

コンセントリック（上げる）動作の後半では、バーを止めるための制動が必ず発生する。
この制動区間では実効負荷が `m×g` を下回り、筋肉への負荷が抜ける。

```
実効負荷 = m × (g + a_vertical)

  上げ始め（加速）:  a > 0 → 実効負荷 > mg  （負荷が乗る）
  上げ終わり（制動）: a < 0 → 実効負荷 < mg  （負荷が抜ける）
```

速く上げるほどこの制動区間が長くなる（軽い重量では最大52%が制動区間）。

**③ ゆっくり上げると何が起きるか**

- 収縮速度が低い → Hill方程式から最大力に近い力を発揮し続ける
- 制動区間が短い、または等速に近くなる → 負荷が抜けにくい
- TUT（緊張時間）が長くなる → 機械的張力の積算が増える

**結論**: ゆっくり8回の方が「筋肉が高い負荷に晒された総時間・総量」は多い。
この「総量」を数値化するのが本アプリのコアである。

---

## 3. 指標の定義

### 3.1 Effective Load Work（ELW）— セットの総負荷量

1レップあたりの「実効負荷 × 速度」を時間積分し、符号を取らずに積算：

```
ELW = Σ ∫ m × (g + a(t)) × |v(t)| dt   [J]
         各レップ
```

- 上げ・下げ両方をカウント（往復でゼロにならない）
- ゆっくり上げると単位時間あたりの負荷が高いまま維持されるため積算値が大きくなる
- 速く上げると制動区間で `(g + a)` が負になり、積算値が小さくなる

### 3.2 Load Quality Score（LQS）— 1レップあたりの質スコア

ELWに「どれだけ負荷をかけ続けられたか」の係数を掛けたスコア：

```
LQS = ELW_rep × load_quality

load_quality = (1 - DR) × (1 + IHD_bonus)
```

**DR（Deceleration Ratio）: 負荷が抜けた割合**

```
DR = 制動区間の時間 / コンセントリック全体の時間   [0〜1]
```

- DR = 0.1 → ほぼ等速で最後まで押せた（よい）
- DR = 0.5 → 半分が制動区間（負荷が抜けている）

**IHD_bonus: 途中で止めたことへのボーナス**

```
IHD_bonus = min(IHD / 2.0, 0.5)

IHD = バーが静止（|v| < 0.02 m/s）していた時間 [s]
```

「50%地点で止めて気合いで再開」などの等尺性保持は、
IMUでは力そのものは計測できないが、止まっている間も重力分の負荷（m×g）が
筋肉にかかっているため、保持時間に応じてボーナスを加算する。

### 3.3 Set Score — セット全体のスコア

```
Set Score = Σ LQS_i   （全レップの合計）
```

**これが最も重要な数値。** 回数ではなくセット全体の「負荷の質の総量」。

「速く10回」 vs 「ゆっくり8回」の比較例：

| | 速く10回 | ゆっくり8回 |
|--|---------|-----------|
| ELW/rep | 低（制動区間が長い） | 高（等速維持） |
| DR/rep | 高（0.4〜0.5） | 低（0.1〜0.2） |
| load_quality | 0.5〜0.6 | 0.8〜0.9 |
| LQS/rep | 小さい | 大きい |
| **Set Score** | **低い** | **高い** |

### 3.4 補助指標（参考表示用）

| 指標 | 定義 | IMU計測 |
|------|------|---------|
| TUT（Time Under Tension） | セットの総緊張時間 [s] | 可 |
| MPV（Mean Propulsive Velocity） | 推進区間（a>0）の平均速度 [m/s] | 可 |
| Velocity Loss | 第1レップから最終レップへの速度低下率 [%] | 可 |
| Estimated Impulse | m × g × IHD（等尺性保持の推定力積） | 可（推定値） |

---

## 4. UIと体験設計

### 4.1 計測中画面

```
╔══════════════════════════════╗
║  ベンチプレス  60kg          ║
║                              ║
║      ● 計測中               ║
║                              ║
║   rep  5                     ║
║                              ║
║   負荷継続率  ████████░░ 82% ║
║   （緑が多いほど良い）        ║
║                              ║
║   Set Score  1,840 pts       ║
╚══════════════════════════════╝
```

- **負荷継続率**（= 1 - DR）をリアルタイムで表示。緑ゲージが多いほど良い動作
- 等尺性保持中は「HOLD ⏱ 1.2s」とタイマー表示してボーナス中であることを示す

### 4.2 セット完了画面（最重要）

回数ではなくスコアで成果を表現する：

```
╔══════════════════════════════╗
║   セット完了                 ║
║                              ║
║        8 reps                ║
║                              ║
║   Set Score  2,140 pts  ✓    ║
║   先週の同セット比  +12%      ║
║                              ║
║   TUT        38.4 sec        ║
║   平均 DR    17%  （良好）    ║
║                              ║
║  「高品質なセットでした」      ║
╚══════════════════════════════╝
```

- 目標回数（10回）に届かなくても、Set Scoreが高ければポジティブに表示
- 「8回しかできなかった」ではなく「質の高い8回」として評価
- 先週比でスコアが上がっていれば成長として表示

### 4.3 レポート画面

#### セット内レップ比較

```
rep  LQS（棒グラフ）    DR
 1   ████████  280     14%
 2   ████████  275     16%
 3   ███████   260     19%
 4   ███████   255     21%
 5   ██████    230     24%  ← 疲れてきた
 6   ██████    225     25%
 7   █████     215     28%
 8   █████     200     30%
```

- レップごとのLQSとDRを並べて疲労の推移を可視化
- DRが上がってきたら「制動が増えてきた = 疲労のサイン」として表示

#### 長期トレンド

- 週次・月次のSet Score推移
- 同じ重量でSet Scoreが上がる = 同じ重量でより質の高いトレーニングができるようになった

---

## 5. デバイス構成

### メインデバイス: iPhone

- **設置**: バーベルシャフトにクランプ型ホルダーでマウント
- **センサー**:
  - 加速度計（100Hz）: 鉛直加速度の計測
  - ジャイロスコープ（100Hz）: バーの傾き補正
  - カメラ（オプション）: 種目の自動認識

### iPhoneのみで成立する理由

- Core Motion（iPhone 12以降）で6軸IMUを100Hz取得可能
- 鉛直加速度の積分で速度・変位を推定（研究検証済み、相関係数0.95以上）
- ドリフト誤差はZUPT（レップ間静止で速度ゼロリセット）で補正

### 将来拡張

| Phase | 拡張内容 | 追加できること |
|-------|---------|--------------|
| 5 | ロードセル / FSRセンサー | 等尺性保持中の実際の発揮力、真のImpulse計測 |
| 5 | M5StickC（BLE） | より精度の高いIMUデータ |
| 5 | Apple Watch連携 | 心拍数との相関分析 |

---

## 6. 技術スタック

- **言語**: Swift
- **Core Motion**: IMUデータ取得（100Hz）
- **SwiftUI**: UI
- **Swift Charts**: グラフ表示
- **SwiftData**: ローカルデータ保存
- **Vision / Core ML**: 種目自動認識（Phase 4）

### データフロー

```
IMU (100Hz)
  → ローパスフィルタ（ノイズ除去）
  → 速度積分 + ZUPT補正
  → レップ検出（速度ゼロクロス）
  → 等尺性保持検出（IHD）
  → ELW / DR / LQS の計算
  → Set Score 更新
  → UI更新（30Hz）/ DB保存
```

---

## 7. 技術的課題と対策

| 課題 | 原因 | 対策 |
|------|------|------|
| 速度積分のドリフト | 加速度誤差の累積 | レップ間静止でゼロリセット（ZUPT） |
| 重量が不明 | IMUで力を直接計測できない | ユーザーが手動入力（バー＋プレート重量） |
| 等尺性中の力が不明 | バー静止中はIMU応答なし | 保持時間×重量でImpulse推定 / 将来ロードセル追加 |
| DRの閾値調整 | 加速度ゼロの瞬間の検出精度 | カルマンフィルタで加速度計とジャイロを融合 |
| マウント安定性 | バー上でiPhoneが動く | クランプ型ホルダー使用、装着位置の補正処理 |

---

## 8. 開発フェーズ

### Phase 1: MVP（コア計測）
- [ ] Core Motionでの加速度・ジャイロ取得（100Hz）
- [ ] ZUPT付き速度積分
- [ ] レップ自動検出
- [ ] 等尺性保持フェーズ検出（IHD）
- [ ] ELW / DR / LQS / Set Score の計算
- [ ] 計測画面（リアルタイム負荷継続率・Set Score）
- [ ] セット完了画面（スコアでポジティブ評価）

### Phase 2: 計測精度向上
- [ ] カルマンフィルタの実装
- [ ] DRパラメータのキャリブレーション
- [ ] バー装着位置の回転補正

### Phase 3: レポート・データ永続化
- [ ] セット内レップ比較グラフ
- [ ] 長期トレンド（Set Score推移）
- [ ] SwiftDataによるデータ保存

### Phase 4: 種目認識
- [ ] IMUパターンによる自動種目認識
- [ ] カメラ＋Vision frameworkによる補完

### Phase 5: センサー拡張
- [ ] ロードセル連携（等尺性の真のImpulse）
- [ ] M5StickC / Apple Watch連携

---

## 9. 競合との差別化

| 機能 | 既存VBTアプリ | 本アプリ |
|------|-------------|---------|
| 速度計測 | ○（多数あり） | ○ |
| Deceleration Ratio（負荷が抜けた割合） | なし | **独自** |
| 等尺性保持のスコア加算 | なし | **独自** |
| 「質×量」の統合スコア（Set Score） | なし | **独自** |
| 回数より質を評価するUI | なし | **独自** |
| 日本語対応 × スマホのみ × 低コスト | なし | **独自** |

既存のVBTアプリ（GymAware, Vitruve等）はすべて「何m/sで上げたか」を計測するツール。
本アプリは「どれだけ負荷をかけ続けられたか」をスコア化する点で根本的に異なる。

---

## 10. 参考文献

- Hill's muscle model: Hill (1938), PMC3840917
- VBT / MPV: NSCA VBT review, PMC7739360
- Deceleration phase ratio: Sanchez-Medina et al. (2010)
- Sticking point: PMC5357260
- IMU validity for barbell velocity: PMC8038306, MDPI sensors 2021
- Isometric impulse as training metric: ScienceDirect (2025)
- Velocity Loss thresholds: PMC7558277
